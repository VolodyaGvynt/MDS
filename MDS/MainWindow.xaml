<Window x:Class="SignalViewerWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:oxy="http://oxyplot.org/wpf"
        Title="Signal Viewer" Height="600" Width="1000">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <oxy:PlotView x:Name="PlotOsc" Grid.Column="0" />
            <oxy:PlotView x:Name="PlotSpec" Grid.Column="1" />
        </Grid>

        <Grid Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Grid.Column="0" Margin="10">
                <TextBlock Text="CSV File:" />
                <TextBox x:Name="FilePathBox" IsReadOnly="True" Height="25" Width="200"/>
                <Button Content="Browse" Click="Browse_Click" Width="100" Margin="0,5,0,0"/>
                <Button Content="Visualize" Click="Visualize_Click" Width="100" Margin="0,5,0,0"/>
            </StackPanel>

            <StackPanel Grid.Row="0" Grid.Column="1" Margin="10">
                <TextBlock Text="Sampling Rate [Hz]:" />
                <TextBox x:Name="SampleRateBox" Text="1000" Width="100" Height="25" />
                <TextBlock Text="Start Index:" Margin="0,10,0,0"/>
                <TextBox x:Name="StartBox" Text="0" Width="100" Height="25" />
                <TextBlock Text="Length:" Margin="0,10,0,0"/>
                <TextBox x:Name="LengthBox" Text="1000" Width="100" Height="25" />
            </StackPanel>

            <StackPanel Grid.Row="0" Grid.Column="2" Margin="10">
                <TextBlock Text="Window:"/>
                <ComboBox x:Name="WindowCombo" Width="120" SelectedIndex="0">
                    <ComboBoxItem Content="Rectangular"/>
                    <ComboBoxItem Content="Hann"/>
                    <ComboBoxItem Content="Hamming"/>
                </ComboBox>
                <TextBlock Text="Channel:" Margin="0,10,0,0"/>
                <ComboBox x:Name="ChannelCombo" Width="120"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
